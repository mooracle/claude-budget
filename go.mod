module github.com/mooracle/claude-budget

go 1.26

// Config parsing (.claude-budget.toml) will add:
//   require github.com/BurntSushi/toml v1.x
// The scaffold is stdlib-only so `go build ./...` works before deps are fetched.
